# Example environment variables for local development and deployment
# Copy this file to .env and fill the values before running the app.

# Connect to Supabase via connection pooling
DATABASE_URL="postgresql://postgres.{your-project-supabase}:[your-password]@aws-1-eu-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database. Used for migrations
DIRECT_URL="postgresql://postgres.{your-project-supabase}:[your-password]@aws-1-eu-west-2.pooler.supabase.com:5432/postgres"
# JWT Configuration
JWT_SECRET="change-me-to-a-strong-secret"
JWT_EXPIRATION_TIME="1d"

# Server configuration
PORT=3000
NODE_ENV=development

# CORS - comma separated list of allowed origins (used by backend)
# Local: http://localhost:5173
# Production: Add your Vercel domain (e.g., https://cash-register-mvp.vercel.app)
ALLOWED_ORIGINS="http://localhost:5173,https://your-app-vercel.vercel.app"

# Notes:
# - Never commit your real .env to the repository. Add .env to .gitignore.
# - For deployment (Render/Render/Fly), set these variables in the platform UI instead of committing them.
# - If you use Prisma, ensure DATABASE_URL is present when running prisma commands that require DB access.
#
# DEPLOYMENT CHECKLIST:
# Render (Backend):
#   - DATABASE_URL="postgresql://postgres.{your-project-supabase}:[your-password]@aws-1-eu-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
#   - DIRECT_URL="postgresql://postgres.{your-project-supabase}:[your-password]@aws-1-eu-west-2.pooler.supabase.com:5432/postgres"
#   - JWT_SECRET (generate a strong random string)
#   - JWT_EXPIRATION_TIME (e.g., "7d")
#   - ALLOWED_ORIGINS=http://localhost:5173,https://your-app-vercel.vercel.app
#   - PORT (Render sets this automatically)
#   - NODE_ENV=production
#
# Vercel (Frontend):
#   - VITE_API_URL=https://your-app.up.Render.app

